from setuptools import setup
setup(
    name='sip_callerid',
    version='0.1',
    description='A simple python app that registers itself to a SIP server and returns the caller ID on incoming calls',
    url='https://github.com/MartyTremblay/sip_callerid',
    author='Marty Tremblay',
    author_email='forkme@thetremblays.ca',
    license='MIT',
    zip_safe=False
)
